To align the local database against the corresponding reference sequences, first convert the reference FASTA files into DIAMOND database format using makedb.sh, and then run the alignments with alignments.sh
